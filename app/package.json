{
  "name": "challenge-ready",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Prop firm challenge training simulator — rule engine, replay, autopsy, readiness score",
  "scripts": {
    "test": "node --test test/*.test.js",
    "dev": "python3 -m http.server 5173"
  }
}
